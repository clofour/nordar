---

- name: Deploy to backend at runtime
  become: true
  hosts: backend
  vars_files:
    - ../shared/network.yaml
  roles:
    - ufw_runtime
    - migrations_runtime
    - pki_runtime
    - backend_runtime
