---

- name: Deploy to database at runtime
  become: true
  hosts: database
  vars_files:
    - ../shared/database.yaml
    - ../shared/network.yaml
    - ./vars/database.yaml
  roles:
    - ufw_runtime
    - etcd_runtime
    - patroni_runtime
