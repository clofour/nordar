name: terraform-deploy
on:
  workflow_dispatch:
    inputs:
      mode:
        description: "Deployment mode"
        default: partial
        type: choice
        options:
          - partial
          - full
jobs:
  deploy:
    name: Terraform Deploy
    runs-on: ubuntu-24.04
    environment: production
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.SPACES_ACCESS_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.SPACES_SECRET_KEY }}
      DIGITALOCEAN_TOKEN: ${{ secrets.DO_API_TOKEN }}
      SPACES_ACCESS_KEY_ID: ${{ secrets.SPACES_ACCESS_ID }}
      SPACES_SECRET_ACCESS_KEY: ${{ secrets.SPACES_SECRET_KEY }}
      TF_VAR_domain: ${{ vars.DOMAIN }}
    defaults:
      run:
        working-directory: ./infra/${{ inputs.mode }}/terraform
    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@5e8dbf3c6d9deaf4193ca7a8fb23f2ac83bb6c85
        with:
          terraform_version: "1.14.7"

      - name: Initialize Terraform project
        run: terraform init

      - name: Run Terraform Plan
        run: terraform plan -out=tfplan

      - name: Run Terraform Apply
        run: terraform apply tfplan
